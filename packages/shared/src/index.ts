import {z} from "zod";
export const signupSchema=z.object({name:z.string().min(2).max(100),email:z.email(),mobile:z.string().regex(/^[6-9]\d{9}$/),password:z.string().min(10).max(128),companyName:z.string().min(2).max(160),city:z.string().min(2).max(80),state:z.string().min(2).max(80),country:z.string().default("India"),industryId:z.string().uuid().optional(),businessType:z.string().max(100).optional(),gstin:z.string().max(30).optional()});
export const loginSchema=z.object({email:z.email(),password:z.string().min(1)});export const moneySchema=z.object({amountMinor:z.coerce.bigint().positive().max(100000000000n)});export const paymentRequestSchema=moneySchema.extend({reference:z.string().max(120).optional(),notes:z.string().max(1000).optional(),proofUrl:z.string().url().optional()});export const purchaseSchema=z.object({leadId:z.string().uuid(),idempotencyKey:z.string().min(16).max(100)});export const investmentSchema=z.object({ruleId:z.string().uuid(),amountMinor:z.coerce.bigint().positive(),idempotencyKey:z.string().min(16).max(100)});export type SignupInput=z.infer<typeof signupSchema>;
