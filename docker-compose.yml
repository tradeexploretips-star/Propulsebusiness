services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: propulse_business
    ports: ["5432:5432"]
    volumes: [propulse_pg:/var/lib/postgresql/data]
volumes:
  propulse_pg:
