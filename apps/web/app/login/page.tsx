"use client";import{useState}from"react";import{useRouter}from"next/navigation";const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:4000";export default function Login(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[msg,setMsg]=useState(""),router=useRouter();async function submit(e:any){e.preventDefault();const r=await fetch(`${API}/v1/auth/login`,{method:"POST",headers:{"content-type":"application/json"},credentials:"include",body:JSON.stringify({email,password})}),x=await r.json();if(r.ok)router.push("/dashboard");else setMsg(x.error?.message??"Unable to sign in")}return <main className="section"><div className="container" style={{maxWidth:520}}><div className="card"><h1>Sign in</h1><form onSubmit={submit}><p><input className="input" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Business email"/></p><p><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/></p><button className="btn primary">Sign in</button><p className="muted">{msg}</p></form></div></div></main>}
