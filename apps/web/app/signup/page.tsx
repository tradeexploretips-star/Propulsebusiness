"use client";import{useState}from"react";import{useRouter}from"next/navigation";const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:4000";export default function Signup(){const[f,setF]=useState<any>({}),[msg,setMsg]=useState(""),router=useRouter(),set=(k:string,v:string)=>setF((x:any)=>({...x,[k]:v}));async function submit(e:any){e.preventDefault();const r=await fetch(`${API}/v1/auth/signup`,{method:"POST",headers:{"content-type":"application/json"},credentials:"include",body:JSON.stringify(f)}),x=await r.json();if(r.ok)router.push("/dashboard");else setMsg(x.error?.message??"Unable to create account")}return <main className="section"><div className="container" style={{maxWidth:680}}><div className="card"><h1>Create your professional account</h1><p className="muted">Propulse Business is designed for professionals and businesses.</p><form onSubmit={submit}>{[["name","Full name"],["email","Business email"],["mobile","Mobile"],["password","Password"],["companyName","Company / business name"],["city","City"],["state","State"],["businessType","Business type"]].map(([k,l])=><p key={k}><input className="input" type={k==="password"?"password":"text"} placeholder={l} value={f[k]??""} onChange={e=>set(k,e.target.value)} required/></p>)}<button className="btn primary">Create account</button><p className="muted">{msg}</p></form></div></div></main>}
