"use client";import{useEffect,useState}from"react";const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:4000";export default function Leads(){const[leads,setLeads]=useState<any[]>([]),[q,setQ]=useState(""),[loading,setLoading]=useState(true);useEffect(()=>{setLoading(true);fetch(`${API}/v1/marketplace/leads?q=${encodeURIComponent(q)}`,{credentials:"include"}).then(r=>r.json()).then(x=>setLeads(x.data??[])).finally(()=>setLoading(false))},[q]);return <main className="section"><div className="container"><h1>Lead marketplace</h1><p className="muted">Browse current professional opportunities. Contact details are protected until purchase.</p><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search leads..."/><div style={{height:18}}/>{loading?<p>Loading opportunities…</p>:<div className="grid">{leads.map(l=><article className="card" key={l.id}><span className="tag">{l.industry}</span><h3>{l.title}</h3><p className="muted">{l.city}, {l.state}</p><p>{l.summary}</p><p className="muted">{l.soldShares} / {l.maxBuyers} shares sold</p><div className="price">₹{(Number(l.priceMinor)/100).toLocaleString("en-IN")}</div><a className="btn primary" href={`/leads/${l.id}`}>View details</a></article>)}</div>}</div></main>}
