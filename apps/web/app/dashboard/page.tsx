"use client";import{useEffect,useState}from"react";const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:4000";export default function Dashboard(){const[d,setD]=useState<any>();useEffect(()=>{fetch(`${API}/v1/account/dashboard`,{credentials:"include"}).then(r=>r.json()).then(x=>setD(x.data))},[]);if(!d)return <main className="dashboard"><div className="container">Loading dashboard…</div></main>;return <main className="dashboard"><div className="container"><h1>Business dashboard</h1><div className="stats"><div className="stat">Wallet<strong>₹{(Number(d.wallet?.balanceMinor??0)/100).toLocaleString("en-IN")}</strong></div><div className="stat">Purchased leads<strong>{d.purchases.length}</strong></div><div className="stat">Investments<strong>{d.investments.length}</strong></div><div className="stat">Orders<strong>{d.orders.length}</strong></div></div><section className="section"><h2>Recent purchases</h2>{d.purchases.length?<div className="grid">{d.purchases.map((p:any)=><div className="card" key={p.id}><span className="tag">{p.lead.industry.name}</span><h3>{p.lead.title}</h3><p>{p.lead.city}, {p.lead.state}</p><p>Share #{p.shareNumber}</p></div>)}</div>:<div className="card">No purchases yet. <a href="/leads">Explore leads →</a></div>}</section></div></main>}
